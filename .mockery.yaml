with-expecter: true
resolve-type-alias: false
disable-version-string: true
issue-845-fix: true
dir: "{{.InterfaceDir}}/mocks"
mockname: "Mock{{.InterfaceName}}"
outpkg: mocks
filename: "mock_{{.InterfaceName}}.go"
packages:
  # Interfaces are declared by their consumer, so mocks live with the consumer.
  interview/internal/httpsvc/service:
    interfaces:
      Repository:
  interview/internal/httpsvc/controller:
    interfaces:
      Service:
  interview/internal/grpcsvc/service:
    interfaces:
      Repository:
  interview/internal/grpcsvc/controller:
    interfaces:
      Service:
